{
  // Levels for every environment live in appsettings.json under
  // Serilog:MinimumLevel. This file holds only what should differ while
  // developing, which today is nothing — uncomment the line below to watch the
  // SQL that BillQueryable and the report aggregates generate.
  "Serilog": {
    "MinimumLevel": {
      "Override": {
        // "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  }
}
